{
  "collectionName": "components_shared_video_embeds",
  "info": {
    "displayName": "Video Embed",
    "icon": "play",
    "description": "Embed videos from YouTube, Vimeo, or other platforms"
  },
  "options": {},
  "attributes": {
    "url": {
      "type": "string",
      "required": true,
      "regex": "^https?:\\/\\/(www\\.|m\\.|music\\.|player\\.)?(youtube\\.com|youtube-nocookie\\.com|youtu\\.be|vimeo\\.com)\\/.+",
      "description": "Paste any YouTube or Vimeo URL (share link, watch URL, shorts, embed, etc.). The API auto-converts it to an embeddable URL and exposes provider/videoId/embedUrl."
    },
    "title": {
      "type": "string",
      "description": "Optional title for the video"
    }
  }
}

